# --- Web / API -----------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# --- Settings & validation -----------------------------------------------------
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

# --- Database ------------------------------------------------------------------
SQLAlchemy>=2.0.41
psycopg[binary]>=3.2.3
alembic>=1.14.0

# --- Security ------------------------------------------------------------------
PyJWT>=2.10.0
bcrypt>=4.2.0

# --- Event bus / cache ---------------------------------------------------------
redis>=5.2.0

# --- Log store -----------------------------------------------------------------
elasticsearch>=8.15.0

# --- Serialization -------------------------------------------------------------
PyYAML>=6.0.2
orjson>=3.10.7

# --- ML (threat detection) -----------------------------------------------------
numpy>=2.1.0
scikit-learn>=1.6.0
joblib>=1.4.2
pandas>=2.2.3

# --- AI agent / SOAR -----------------------------------------------------------
httpx>=0.28.1

# --- Reporting -----------------------------------------------------------------
Jinja2>=3.1.4
fpdf2>=2.8.2

# --- FIM agent (real-mode file integrity monitoring) ---------------------------
watchdog>=6.0.0

# --- Tests ---------------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
